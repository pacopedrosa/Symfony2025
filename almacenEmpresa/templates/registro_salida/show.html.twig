{% extends 'base.html.twig' %}

{% block title %}RegistroSalida{% endblock %}

{% block body %}
    <h1>RegistroSalida</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registro_salida.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ registro_salida.cantidad }}</td>
            </tr>
            <tr>
                <th>Fecha_salida</th>
                <td>{{ registro_salida.fechaSalida ? registro_salida.fechaSalida|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_registro_salida_index') }}">back to list</a>

    <a href="{{ path('app_registro_salida_edit', {'id': registro_salida.id}) }}">edit</a>

    {{ include('registro_salida/_delete_form.html.twig') }}
{% endblock %}
