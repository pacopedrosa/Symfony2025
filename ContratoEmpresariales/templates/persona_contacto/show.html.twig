{% extends 'base.html.twig' %}

{% block title %}PersonaContacto{% endblock %}

{% block body %}
    <h1>PersonaContacto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ persona_contacto.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ persona_contacto.name }}</td>
            </tr>
            <tr>
                <th>Puesto</th>
                <td>{{ persona_contacto.puesto }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ persona_contacto.telefono }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ persona_contacto.email }}</td>
            </tr>
            <tr>
                <th>Visible</th>
                <td>{{ persona_contacto.visible }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_persona_contacto_index') }}">back to list</a>

    <a href="{{ path('app_persona_contacto_edit', {'id': persona_contacto.id}) }}">edit</a>

    {{ include('persona_contacto/_delete_form.html.twig') }}
{% endblock %}
