<form method="post" action="{{ path('app_persona_contacto_delete', {'id': persona_contacto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona_contacto.id) }}">
    <button class="btn">Delete</button>
</form>
