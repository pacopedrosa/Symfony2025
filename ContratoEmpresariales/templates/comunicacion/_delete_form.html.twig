<form method="post" action="{{ path('app_comunicacion_delete', {'id': comunicacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comunicacion.id) }}">
    <button class="btn">Delete</button>
</form>
